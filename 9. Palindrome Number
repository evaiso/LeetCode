class Solution(object):
    def isPalindrome(self, x):
        x = str(x)
        x = list(x)
        x.reverse()
        y = list(map(str, x))
        x.reverse()
        x = list(map(str, x))
        if x == y:
            return True
        else:
            return False
